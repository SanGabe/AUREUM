import { notFound } from "next/navigation";
import { EnglishMarketingShell, EnglishPageHero, englishMarketingStyles as styles } from "@/components/english-marketing-shell";
import { parseEnglishLocale } from "@/i18n/locales";
export const metadata = { title: "Pricing" };
export default async function Page({ params }: { params: Promise<{ locale: string }> }) { const locale = parseEnglishLocale((await params).locale); if (!locale) notFound(); return <EnglishMarketingShell locale={locale}><EnglishPageHero eyebrow="PLANS" title="A plan proportional to your organisation." description="Commercial limits and prices are still under validation. No charge will be made without clear plan information and confirmation." /><section className={styles.content}><div className={styles.cards3}>{[["01","Essential","Personal organisation, accounts, categories, goals and monthly view."],["02","Nucleus","Collaboration, roles, approvals and shared organisation."],["03","Automation","Imports, WhatsApp and advanced features as they become available."]].map(([n,t,d]) => <article className={styles.card} key={t}><span className={styles.number}>{n}</span><h3>{t}</h3><p>{d}</p></article>)}</div></section></EnglishMarketingShell>; }
