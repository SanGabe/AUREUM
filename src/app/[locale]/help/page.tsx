import { notFound } from "next/navigation";
import { EnglishMarketingShell, EnglishPageHero, englishMarketingStyles as styles } from "@/components/english-marketing-shell";
import { parseEnglishLocale } from "@/i18n/locales";
export const metadata = { title: "Help centre" };
export default async function Page({ params }: { params: Promise<{ locale: string }> }) { const locale = parseEnglishLocale((await params).locale); if (!locale) notFound(); const items=[["Account and access","Registration, email confirmation, password, profile and closure."],["Financial Nuclei","Creation, code requests, roles and approvals."],["Transactions","Accounts, cards, categories, entries and currencies."],["Imports","Accepted files, duplicates, review and document privacy."],["WhatsApp","Secure linking, temporary codes and document reception."],["Privacy","Rights, security, retention and request channels."]]; return <EnglishMarketingShell locale={locale}><EnglishPageHero eyebrow="HELP" title="Clear answers for each step." description="The help centre is being prepared alongside the product flows." /><section className={styles.content}><div className={styles.cards3}>{items.map(([t,d],i)=><article className={styles.card} key={t}><span className={styles.number}>{String(i+1).padStart(2,"0")}</span><h3>{t}</h3><p>{d}</p></article>)}</div></section></EnglishMarketingShell>; }
