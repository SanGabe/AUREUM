.shell {
  min-height: 100vh;
  display: grid;
  grid-template-columns: 270px 1fr;
  color: var(--a-text);
  background: var(--a-page-bg);
}

.sidebar {
  position: sticky;
  top: 0;
  height: 100vh;
  display: flex;
  flex-direction: column;
  padding: 25px 20px 18px;
  border-right: 1px solid var(--a-border);
  background: linear-gradient(180deg,var(--a-sidebar-1),var(--a-sidebar-2));
}

.brand {
  display: block;
  width: 205px;
  height: 80px;
  margin-bottom: 38px;
}

.brand img {
  width: 100%;
  height: 100%;
  object-fit: contain;
  object-position: left center;
}

.sidebarNav {
  display: grid;
  gap: 5px;
}

.sidebarNav a {
  min-height: 43px;
  display: flex;
  align-items: center;
  gap: 10px;
  padding: 0 12px;
  border-radius: 8px;
  color: var(--a-muted);
  text-decoration: none;
  font-size: 13px;
}

.sidebarNav a:hover,
.sidebarNav .activeLink {
  color: var(--a-gold-strong);
  background: var(--a-gold-soft);
}

.profileWrap {
  position: relative;
  margin-top: auto;
}

.profileButton {
  width: 100%;
  min-height: 65px;
  display: grid;
  grid-template-columns: 39px minmax(0,1fr) 18px;
  gap: 10px;
  align-items: center;
  padding: 13px 8px 4px 0;
  border: 0;
  border-top: 1px solid var(--a-border-soft);
  color: var(--a-text);
  background: transparent;
  text-align: left;
  cursor: pointer;
}

.profileAvatar,
.demoUser > span {
  width: 37px;
  height: 37px;
  display: grid;
  place-items: center;
  border-radius: 50%;
  color: var(--a-page-bg-2);
  background: linear-gradient(135deg,var(--a-gold-strong),#c98c20);
  font-size: 11px;
  font-weight: 900;
}

.profileButtonCopy {
  min-width: 0;
  display: grid;
  gap: 2px;
}

.profileButtonCopy strong {
  font-size: 12px;
}

.profileButtonCopy small {
  overflow: hidden;
  color: var(--a-muted);
  font-size: 9px;
  white-space: nowrap;
  text-overflow: ellipsis;
}

.profileChevron {
  color: var(--a-muted-2);
  font-size: 13px;
  text-align: center;
}

.profileMenu {
  position: absolute;
  z-index: 50;
  right: -8px;
  bottom: calc(100% + 10px);
  left: -8px;
  overflow: hidden;
  padding: 10px;
  border: 1px solid var(--a-border);
  border-radius: 13px;
  background: var(--a-elevated);
  box-shadow: 0 24px 55px rgba(0,0,0,.42);
}

.profileMenuIdentity {
  display: grid;
  grid-template-columns: 39px 1fr;
  gap: 10px;
  align-items: center;
  padding: 5px 5px 9px;
}

.profileMenuIdentity > span {
  width: 37px;
  height: 37px;
  display: grid;
  place-items: center;
  border-radius: 50%;
  color: var(--a-page-bg-2);
  background: var(--a-gold);
  font-size: 11px;
  font-weight: 900;
}

.profileMenuIdentity div {
  min-width: 0;
  display: grid;
  gap: 2px;
}

.profileMenuIdentity strong {
  overflow: hidden;
  font-size: 12px;
  white-space: nowrap;
  text-overflow: ellipsis;
}

.profileMenuIdentity small {
  overflow: hidden;
  color: var(--a-muted);
  font-size: 9px;
  white-space: nowrap;
  text-overflow: ellipsis;
}

.profileDivider {
  height: 1px;
  margin: 7px 0;
  background: var(--a-border-soft);
}

.profileNucleus {
  display: grid;
  gap: 6px;
  padding: 4px;
}

.profileNucleus > span {
  color: var(--a-gold);
  font-size: 8px;
  font-weight: 850;
  letter-spacing: .14em;
}

.profileNucleus select {
  width: 100%;
  min-height: 38px;
  padding: 0 9px;
  border: 1px solid var(--a-border);
  border-radius: 7px;
  outline: 0;
  color: var(--a-text);
  background: var(--a-input);
  font-size: 10px;
}

.profileNucleus option {
  color: var(--a-page-bg-2);
}

.profileLinks {
  display: grid;
  gap: 2px;
}

.profileLinks a {
  display: grid;
  grid-template-columns: 25px 1fr;
  gap: 8px;
  align-items: center;
  padding: 9px 7px;
  border-radius: 7px;
  color: var(--a-text);
}

.profileLinks a:hover {
  color: var(--a-gold-strong);
  background: var(--a-gold-soft);
}

.profileLinks > a > span {
  color: var(--a-gold);
  text-align: center;
}

.profileLinks div {
  display: grid;
  gap: 2px;
}

.profileLinks strong {
  font-size: 10px;
  font-weight: 650;
}

.profileLinks small {
  color: var(--a-muted-2);
  font-size: 8px;
}

.profileLogout {
  width: 100%;
  min-height: 39px;
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 0 8px;
  border: 0;
  border-radius: 7px;
  color: var(--a-danger);
  background: transparent;
  font: inherit;
  font-size: 10px;
  cursor: pointer;
}

.profileLogout:hover {
  background: rgba(220,90,80,.07);
}

.profileLogout:disabled {
  opacity: .55;
  cursor: wait;
}

.demoUser {
  margin-top: auto;
  display: flex;
  gap: 10px;
  align-items: center;
  padding-top: 20px;
  border-top: 1px solid var(--a-border-soft);
}

.demoUser div {
  display: grid;
  gap: 2px;
}

.demoUser strong {
  font-size: 12px;
}

.demoUser small {
  color: var(--a-muted);
  font-size: 9px;
}

.content {
  min-width: 0;
  padding: 42px clamp(24px,4vw,65px) 70px;
}

.top {
  display: flex;
  justify-content: space-between;
  gap: 25px;
  align-items: flex-start;
  margin-bottom: 19px;
}

.eyebrow {
  margin: 0 0 7px;
  color: var(--a-gold);
  font-size: 10px;
  font-weight: 800;
  letter-spacing: .16em;
}

.top h1 {
  margin: 0;
  color: var(--a-heading);
  font-family: Georgia,serif;
  font-size: clamp(2.4rem,5vw,4.2rem);
  font-weight: 500;
  letter-spacing: -.04em;
}

.top p:not(.eyebrow) {
  margin: 8px 0 0;
  color: var(--a-muted);
  font-size: 13px;
}

.liveBadge,
.demoBadge {
  margin-top: 8px;
  padding: 8px 11px;
  border-radius: 99px;
  font-size: 9px;
  font-weight: 850;
  letter-spacing: .1em;
}

.liveBadge {
  color: var(--a-success);
  border: 1px solid rgba(91,214,164,.25);
  background: rgba(91,214,164,.06);
}

.demoBadge {
  color: var(--a-gold-strong);
  border: 1px solid var(--a-border);
}

.periodBar {
  position: relative;
  z-index: 20;
  display: flex;
  justify-content: flex-start;
  margin-bottom: 16px;
}

.monthNavigator {
  position: relative;
  display: grid;
  gap: 6px;
}

.periodLabel {
  color: var(--a-muted-2);
  font-size: 8px;
  font-weight: 850;
  letter-spacing: .14em;
}

.monthControl {
  display: grid;
  grid-template-columns: 34px minmax(190px,auto) 34px;
  align-items: stretch;
}

.monthArrow,
.monthMain {
  min-height: 40px;
  border: 1px solid var(--a-border);
  color: var(--a-text);
  background: var(--a-surface-2);
  cursor: pointer;
}

.monthArrow {
  display: grid;
  place-items: center;
  color: var(--a-gold);
  font-family: Georgia,serif;
  font-size: 22px;
}

.monthArrow:first-child {
  border-radius: 8px 0 0 8px;
}

.monthArrow:last-child {
  border-radius: 0 8px 8px 0;
}

.monthMain {
  display: grid;
  grid-template-columns: 18px 1fr 15px;
  gap: 8px;
  align-items: center;
  padding: 0 11px;
  border-right: 0;
  border-left: 0;
}

.monthMain strong {
  font-size: 10px;
  letter-spacing: .045em;
  text-align: left;
}

.calendarIcon,
.chevron {
  color: var(--a-gold);
}

.monthPopover {
  position: absolute;
  z-index: 60;
  top: calc(100% + 9px);
  left: 34px;
  width: 310px;
  padding: 13px;
  border: 1px solid var(--a-border);
  border-radius: 12px;
  background: var(--a-elevated);
  box-shadow: 0 24px 55px rgba(0,0,0,.4);
}

.monthPopoverHeader {
  display: grid;
  grid-template-columns: 34px 1fr 34px;
  align-items: center;
  margin-bottom: 10px;
}

.monthPopoverHeader strong {
  color: var(--a-heading);
  font-family: Georgia,serif;
  font-size: 17px;
  font-weight: 500;
  text-align: center;
}

.monthPopoverHeader button {
  width: 32px;
  height: 32px;
  border: 1px solid var(--a-border);
  border-radius: 7px;
  color: var(--a-gold);
  background: var(--a-surface-3);
  cursor: pointer;
}

.monthGrid {
  display: grid;
  grid-template-columns: repeat(3,1fr);
  gap: 6px;
}

.monthGrid button {
  min-height: 38px;
  border: 1px solid var(--a-border-soft);
  border-radius: 7px;
  color: var(--a-muted);
  background: var(--a-surface-3);
  font-size: 9px;
  font-weight: 750;
  cursor: pointer;
}

.monthGrid button:hover,
.monthGrid .monthActive {
  color: var(--a-page-bg-2);
  border-color: var(--a-gold);
  background: linear-gradient(135deg,var(--a-gold-strong),#d69b29);
}

.todayButton {
  width: 100%;
  min-height: 35px;
  margin-top: 9px;
  border: 1px solid var(--a-border);
  border-radius: 7px;
  color: var(--a-gold-strong);
  background: transparent;
  font-size: 9px;
  cursor: pointer;
}

.summary {
  display: grid;
  grid-template-columns: repeat(4,1fr);
  gap: 13px;
  margin-bottom: 13px;
}

.summary article {
  min-height: 135px;
  display: grid;
  align-content: center;
  gap: 7px;
  padding: 20px;
  border: 1px solid var(--a-border);
  border-radius: 12px;
  background: linear-gradient(145deg,var(--a-surface-2),var(--a-surface-3));
}

.summary span {
  color: var(--a-muted);
  font-size: 11px;
}

.summary strong {
  color: var(--a-heading);
  font-size: clamp(1.35rem,2.4vw,2rem);
}

.summary small {
  color: var(--a-muted-2);
  font-size: 10px;
}

.positive {
  color: var(--a-success) !important;
}

.negative {
  color: var(--a-danger) !important;
}

.currencyNote {
  margin: 0 0 13px;
  padding: 10px 13px;
  border: 1px solid var(--a-border);
  border-radius: 8px;
  color: #b5a470;
  background: var(--a-gold-soft);
  font-size: 11px;
}

.grid {
  display: grid;
  grid-template-columns: 1.45fr .75fr;
  gap: 13px;
}

.panel {
  min-height: 290px;
  padding: 22px;
  border: 1px solid var(--a-border);
  border-radius: 12px;
  background: var(--a-surface);
}

.wide {
  min-width: 0;
}

.panelHead {
  display: flex;
  justify-content: space-between;
  gap: 15px;
  align-items: flex-start;
  margin-bottom: 18px;
}

.panelHead p {
  margin: 0 0 4px;
  color: var(--a-gold);
  font-size: 9px;
  font-weight: 800;
  letter-spacing: .14em;
  text-transform: uppercase;
}

.panelHead h2 {
  margin: 0;
  color: var(--a-heading);
  font-family: Georgia,serif;
  font-size: 22px;
  font-weight: 500;
}

.panelHead > span {
  color: var(--a-gold-strong);
  font-family: Georgia,serif;
  font-size: 22px;
}

.transactions {
  display: grid;
}

.transaction {
  display: grid;
  grid-template-columns: 36px 1fr auto;
  gap: 11px;
  align-items: center;
  padding: 12px 0;
  border-top: 1px solid var(--a-border-soft);
}

.transaction:first-child {
  border-top: 0;
}

.txIcon {
  width: 31px;
  height: 31px;
  display: grid;
  place-items: center;
  border: 1px solid var(--a-border);
  border-radius: 8px;
  color: var(--a-gold);
}

.transaction > div:nth-child(2) {
  display: grid;
  gap: 3px;
}

.transaction strong {
  font-size: 12px;
}

.transaction small {
  color: #7387a2;
  font-size: 9px;
}

.txValue {
  display: grid;
  gap: 3px;
  text-align: right;
}

.goalAmount {
  display: grid;
  gap: 3px;
  margin: 25px 0 16px;
}

.goalAmount strong {
  color: var(--a-heading);
  font-size: 28px;
}

.goalAmount span {
  color: #8496ad;
  font-size: 11px;
}

.progress {
  height: 6px;
  overflow: hidden;
  border-radius: 99px;
  background: #0c2a49;
}

.progress i {
  display: block;
  height: 100%;
  border-radius: 99px;
  background: linear-gradient(90deg,#d99d2e,#f2c75f);
}

.categories {
  display: grid;
  gap: 16px;
}

.category {
  display: grid;
  grid-template-columns: 160px 1fr 40px;
  gap: 12px;
  align-items: center;
}

.category > div:first-child {
  display: flex;
  justify-content: space-between;
  gap: 8px;
}

.category strong {
  font-size: 11px;
}

.category span {
  color: var(--a-muted);
  font-size: 10px;
}

.empty {
  min-height: 160px;
  display: grid;
  place-content: center;
  text-align: center;
  padding: 20px;
}

.empty strong {
  color: #cdd6e1;
}

.empty p {
  max-width: 390px;
  margin: 7px auto 0;
  color: #70849f;
  font-size: 11px;
  line-height: 1.5;
}

.structure {
  display: grid;
  grid-template-columns: repeat(3,1fr);
  gap: 8px;
  margin-top: 30px;
}

.structure div {
  display: grid;
  gap: 4px;
  text-align: center;
  padding: 17px 8px;
  border: 1px solid var(--a-border);
  border-radius: 9px;
  background: #051d38;
}

.structure strong {
  color: var(--a-gold-strong);
  font-size: 24px;
}

.structure span {
  color: #8496ad;
  font-size: 10px;
}

@media(max-width:1050px) {
  .shell {
    grid-template-columns: 220px 1fr;
  }

  .sidebar {
    padding-inline: 14px;
  }

  .brand {
    width: 175px;
  }

  .summary {
    grid-template-columns: repeat(2,1fr);
  }

  .grid {
    grid-template-columns: 1fr;
  }
}

@media(max-width:720px) {
  .shell {
    display: block;
  }

  .sidebar {
    position: relative;
    height: auto;
  }

  .sidebarNav {
    grid-template-columns: repeat(2,1fr);
  }

  .profileWrap,
  .demoUser {
    margin-top: 18px;
  }

  .profileMenu {
    right: 0;
    bottom: calc(100% + 8px);
    left: 0;
  }

  .content {
    padding: 30px 16px 60px;
  }

  .top {
    display: grid;
  }

  .periodBar {
    z-index: 30;
  }

  .monthPopover {
    left: 0;
    width: min(310px,calc(100vw - 32px));
  }

  .summary {
    grid-template-columns: 1fr;
  }

  .transaction {
    grid-template-columns: 32px 1fr;
  }

  .txValue {
    grid-column: 2;
    text-align: left;
  }

  .category {
    grid-template-columns: 1fr;
  }

  .structure {
    grid-template-columns: repeat(3,1fr);
  }
}

/* ------------------------------------------------------------------ */
/* Dashboard action loading popup                                     */
/* ------------------------------------------------------------------ */

.actionLoadingOverlay {
  position: fixed;
  z-index: 2147483000;
  inset: 0;
  display: grid;
  place-items: center;
  padding: 24px;
  background: rgba(0, 10, 25, .72);
  backdrop-filter: blur(5px);
  -webkit-backdrop-filter: blur(5px);
  animation: aureumDashboardOverlayIn 140ms ease-out both;
}

.actionLoadingCard {
  min-width: 310px;
  max-width: calc(100vw - 36px);
  display: grid;
  justify-items: center;
  gap: 20px;
  padding: 28px 34px 25px;
  border: 1px solid rgba(228,170,50,.28);
  border-radius: 16px;
  background:
    radial-gradient(circle at 50% 0%,var(--a-gold-soft),transparent 12rem),
    #041830;
  box-shadow:
    0 28px 70px rgba(0,0,0,.45),
    inset 0 1px 0 rgba(255,255,255,.025);
}

.actionLoadingOrbit {
  --orbit-size: 76px;
  position: relative;
  width: var(--orbit-size);
  height: var(--orbit-size);
}

.actionLoadingRing {
  position: absolute;
  inset: 8px;
  border: 1px solid rgba(228,170,50,.4);
  border-radius: 50%;
  box-shadow:
    0 0 24px rgba(228,170,50,.08),
    inset 0 0 12px rgba(228,170,50,.025);
}

.actionLoadingRing::after {
  content: "";
  position: absolute;
  inset: 8px;
  border: 1px solid rgba(228,170,50,.09);
  border-radius: 50%;
}

.actionLoadingDot {
  position: absolute;
  inset: 0;
  animation: aureumDashboardOrbit 850ms linear infinite;
}

.actionLoadingDot::before {
  content: "";
  position: absolute;
  top: 1px;
  left: 50%;
  width: 10px;
  height: 10px;
  border-radius: 50%;
  background: linear-gradient(135deg,#f8d978,#d99d2e);
  box-shadow:
    0 0 11px rgba(241,195,93,.75),
    0 0 24px rgba(228,170,50,.34);
  transform: translateX(-50%);
}

.actionLoadingMark {
  position: absolute;
  inset: 0;
  display: grid;
  place-items: center;
  color: var(--a-gold);
  font-family: Georgia,serif;
  font-size: 20px;
  opacity: .72;
}

.actionLoadingCopy {
  display: grid;
  gap: 5px;
  text-align: center;
}

.actionLoadingCopy strong {
  color: var(--a-heading);
  font-family: Georgia,serif;
  font-size: 19px;
  font-weight: 500;
}

.actionLoadingCopy span {
  color: var(--a-muted);
  font-size: 10px;
}

@keyframes aureumDashboardOrbit {
  to {
    transform: rotate(360deg);
  }
}

@keyframes aureumDashboardOverlayIn {
  from {
    opacity: 0;
  }

  to {
    opacity: 1;
  }
}

@media(max-width:520px) {
  .actionLoadingCard {
    min-width: 0;
    width: min(300px,calc(100vw - 32px));
    padding-inline: 24px;
  }

  .actionLoadingOrbit {
    --orbit-size: 70px;
  }
}

@media(prefers-reduced-motion:reduce) {
  .actionLoadingOverlay {
    animation: none;
  }

  .actionLoadingDot {
    animation-duration: 1500ms;
  }
}

/* AUREUM GLOBAL UX DEMO V2 */
.shell {
  grid-template-columns: 248px minmax(0,1fr);
  color: var(--a-text);
  background: var(--a-page-bg);
}

.sidebar {
  padding: 24px 18px 18px;
  border-color: var(--a-border);
  background: linear-gradient(180deg,var(--a-sidebar-1),var(--a-sidebar-2));
}

.brand {
  width: 188px;
  height: 72px;
  margin-bottom: 30px;
}

.sidebarNav {
  gap: 6px;
}

.sidebarNav a {
  min-height: 44px;
  gap: 10px;
  padding: 0 12px;
  color: var(--a-muted);
  font-size: 12px;
}

.sidebarNav a:hover,
.sidebarNav .activeLink {
  color: var(--a-gold-strong);
  background: var(--a-gold-soft);
}

.content {
  width: min(100%,1560px);
  margin: 0 auto;
  padding: 38px clamp(28px,3.6vw,58px) 64px;
}

.profileButton,
.profileMenu {
  color: var(--a-text);
}

.profileMenu {
  overflow: visible;
  border-color: var(--a-border);
  background: var(--a-elevated);
  box-shadow: var(--a-shadow);
}

.profileNucleus > span {
  color: var(--a-gold);
  font-size: 9px;
}

.profileNucleus select {
  min-height: 42px;
  padding: 0 11px;
  color: var(--a-control-text);
  border-color: var(--a-control-border);
  background: var(--a-control-bg);
  font-size: 11px;
}

.profileNucleus option,
.profileNucleus optgroup {
  color: var(--a-option-text);
  background: var(--a-option-bg);
}

.profileLinks strong {
  font-size: 11px;
}

.profileLinks small {
  color: var(--a-muted-2);
  font-size: 9px;
}

.top {
  margin-bottom: 20px;
}

.top h1 {
  font-size: clamp(3rem,4.8vw,4.6rem);
}

.top p:not(.eyebrow) {
  max-width: 780px;
  color: var(--a-muted);
  font-size: 13px;
  line-height: 1.55;
}

.eyebrow {
  color: var(--a-gold);
  font-size: 10px;
}

.periodLabel {
  color: var(--a-muted-2);
  font-size: 9px;
}

.monthControl {
  grid-template-columns: 40px minmax(210px,auto) 40px;
}

.monthArrow,
.monthMain {
  min-height: 44px;
  color: var(--a-control-text);
  border-color: var(--a-control-border);
  background: var(--a-control-bg);
}

.monthMain strong {
  font-size: 11px;
}

.monthPopover {
  width: 330px;
  color: var(--a-text);
  border-color: var(--a-border);
  background: var(--a-elevated);
  box-shadow: var(--a-shadow);
}

.monthPopoverHeader button,
.monthGrid button {
  color: var(--a-text);
  border-color: var(--a-border-soft);
  background: var(--a-surface-2);
}

.summary {
  gap: 12px;
}

.summary article {
  min-height: 128px;
  padding: 18px;
  color: var(--a-text);
  border-color: var(--a-border);
  background: var(--a-card-gradient);
}

.summary span {
  color: var(--a-muted);
  font-size: 11px;
}

.summary strong {
  font-size: clamp(1.45rem,2vw,2rem);
}

.summary small {
  color: var(--a-muted-2);
  font-size: 9px;
}

.panel {
  color: var(--a-text);
  border-color: var(--a-border);
  background: var(--a-surface);
  box-shadow: var(--a-shadow);
}

.panelHead p {
  color: var(--a-gold);
  font-size: 9px;
}

.panelHead h2 {
  color: var(--a-heading);
  font-size: 20px;
}

.transaction strong,
.category strong {
  font-size: 11px;
}

.transaction small,
.category span {
  font-size: 9px;
}

@media(min-width:1600px) {
  .shell {
    grid-template-columns: 260px minmax(0,1fr);
  }

  .content {
    width: min(100%,1640px);
    padding-inline: clamp(42px,4vw,74px);
  }
}

@media(max-width:1180px) {
  .shell {
    grid-template-columns: 220px minmax(0,1fr);
  }

  .sidebar {
    padding-inline: 14px;
  }

  .brand {
    width: 170px;
  }

  .content {
    padding-inline: 24px;
  }
}

@media(max-width:1024px) {
  .shell {
    display: block;
  }

  .sidebar {
    display: none;
  }

  .content {
    width: 100%;
    padding: 28px 20px 112px;
  }
}

@media(max-width:700px) {
  .content {
    padding: 20px 12px 108px;
  }

  .top h1 {
    font-size: clamp(2.3rem,12vw,3.5rem);
  }
}

/* AUREUM PROFILE AUTH V3 */
.profileMenuIdentity > span,
.profileAvatar {
  overflow: hidden;
}

.profileMenuIdentity > span img,
.profileAvatar img {
  width: 100%;
  height: 100%;
  display: block;
  object-fit: cover;
}
